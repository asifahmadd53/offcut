import { AppShell } from '@/components/AppShell'

/** Full Add leftover by hand screen is built in Stage 7. */
export default function AddLeftover() {
  return (
    <AppShell title="Add leftover by hand" back="/stock">
      <p className="mt-8 text-center text-[15px] text-muted-foreground">Add leftover — coming soon.</p>
    </AppShell>
  )
}
