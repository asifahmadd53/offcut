import { AppShell } from '@/components/AppShell'

/** Full Leftover detail screen is built in Stage 7. */
export default function LeftoverDetail() {
  return (
    <AppShell title="Leftover" back="/stock">
      <p className="mt-8 text-center text-[15px] text-muted-foreground">Leftover detail — coming soon.</p>
    </AppShell>
  )
}
