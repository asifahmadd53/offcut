import { AppShell } from '@/components/AppShell'

/** Full Settings screen is built in Stage 8. */
export default function Settings() {
  return (
    <AppShell title="Settings" tab="settings">
      <p className="mt-8 text-center text-[15px] text-muted-foreground">Settings — coming soon.</p>
    </AppShell>
  )
}
