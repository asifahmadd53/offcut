import { AppShell } from '@/components/AppShell'

/** Full Cutting plan screen is built in Stage 5/6. */
export default function Plan() {
  return (
    <AppShell title="Cutting plan" back="/new">
      <p className="mt-8 text-center text-[15px] text-muted-foreground">Plan — coming soon.</p>
    </AppShell>
  )
}
