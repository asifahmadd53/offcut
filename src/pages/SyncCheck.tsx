import { AppShell } from '@/components/AppShell'

/** Full Sync check screen is built in Stage 9. */
export default function SyncCheck() {
  return (
    <AppShell title="Sync check" back="/">
      <p className="mt-8 text-center text-[15px] text-muted-foreground">Sync check — coming soon.</p>
    </AppShell>
  )
}
