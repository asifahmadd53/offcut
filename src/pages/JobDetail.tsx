import { AppShell } from '@/components/AppShell'

/** Full Job detail screen is built in Stage 7. */
export default function JobDetail() {
  return (
    <AppShell title="Job" back="/">
      <p className="mt-8 text-center text-[15px] text-muted-foreground">Job detail — coming soon.</p>
    </AppShell>
  )
}
